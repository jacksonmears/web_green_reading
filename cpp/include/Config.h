#pragma once

namespace config {
inline constexpr float SCALE = 0.5f;
inline constexpr int SCREEN_WIDTH = 800;
inline constexpr int SCREEN_HEIGHT = 600;
inline constexpr float Y_OFFSET = 0.25f;

inline constexpr float ARROW_SIZE = 0.5f * SCALE;
}

